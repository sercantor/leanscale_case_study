import 'package:flutter/material.dart';

class FullCourseScreen extends StatelessWidget {
  static const route = '/fullcourse';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('fullcourse'),
    );
  }
}
